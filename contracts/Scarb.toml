[package]
name = "legacy_vault"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.12.0"
openzeppelin_token = "1.0.0"

[dev-dependencies]
snforge_std = "0.63.0"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]

[scripts]
test = "snforge test"

[[target.starknet-contract]]
sierra = true
casm = true
